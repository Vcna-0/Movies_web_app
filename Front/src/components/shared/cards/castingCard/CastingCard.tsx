import { GenericCardProps } from '../genericCard/GenericCard';
import { StyledCastingCard } from './CastingCardStyles';

export default function CastingCard(props: GenericCardProps) {
   return <StyledCastingCard {...props} />;
}
